// Lists we use as fallbacks on chains that our backend doesn't support
const _COINGECKO_AVAX_LIST = 'https://tokens.coingecko.com/avalanche/all.json'

export const DEFAULT_INACTIVE_LIST_URLS: string[] = []
